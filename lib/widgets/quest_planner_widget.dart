import 'package:flutter/material.dart';

class QuestPlannerWidget extends StatelessWidget {
  const QuestPlannerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Quest Planner Widget');
  }
}
